#!/usr/bin/env bash

# git reset --hard origin/master
npm run test

git checkout head~2 -- test

[ -d test/module ] && {
  npm run test -- --recursive test/module
    # if this fails
      npm version major -m "%s"
      exit 0

  npm run test -- $(find test -type f -name *.js -not -path test/module/\*)
}
